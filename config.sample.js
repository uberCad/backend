/**
 * Enter your values in blank lines and rename config.sample.js to config.js
 * @type {string}
 */

// const hostname = `http://localhost:8529/`;  //for work calculate on localhost
const hostname = `https://uber-cad.ml/`;

const calculateSpreadsheetId = ``;
const calculateApiKey = ``;

module.exports = {
    hostname,
    calculateSpreadsheetId,
    calculateApiKey
};